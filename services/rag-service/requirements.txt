# FastAPI and server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Pydantic and settings
pydantic==2.7.4
pydantic-settings==2.3.4

# Async MongoDB
motor==3.4.0
pymongo==4.7.3

# LangGraph and LangChain
langgraph==0.2.0
langchain-core>=0.2.27
langchain>=0.2.5
langchain-community>=0.2.5
langchain-openai==0.1.9
langchain-anthropic==0.1.15

# LLM Providers
openai==1.35.3
anthropic==0.29.0
tiktoken==0.7.0

# HTTP client
httpx==0.27.0

# TypeSafe Jev (optional). The Jev judge uses httpx against POST /v1/systemone
# by default. Install typesafe-sdk if you prefer the official client locally:
# typesafe-sdk>=0.7.0

# Server-Sent Events for streaming
sse-starlette==2.1.0

# Experiment tracking (optional)
mlflow==2.19.0

# Redis for caching
redis==5.0.4

# JWT (required by rag_config_common)
python-jose[cryptography]==3.3.0

# Utilities
python-dotenv==1.0.1
tenacity==8.4.1

# Shared models (installed separately in Dockerfile)
# rag-config-common @ file:../../shared/python

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Observability (optional)
langfuse>=2.0.0  # optional, for observability

# Development
mypy==1.10.0
